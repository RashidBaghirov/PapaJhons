@model Rules

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Rules</h4>
    <hr />
    <dl class="row m-3">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Rule)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Rule)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn waves-effect waves-light btn-danger btn-outline-danger" />
        <a asp-action="Index" class="btn waves-effect waves-light btn-inverse">Back to List</a>
    </form>
</div>
